/**
 *   Development configs
 */

export const mongooseConfig = Object.freeze({
  DB_URL: "mongodb://localhost:27017/chillangDatabase",
});

export const serverConfig = Object.freeze({
  PORT: 3000,
});
